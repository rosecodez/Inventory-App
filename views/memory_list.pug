extends layout

block content
  h1= title
  if memoryList && memoryList.length
    ul
      each memory in memoryList
        li
          a(href=`/memorys/${memory._id}`)= `${memory.brand} ${memory.type} ${memory.series} ${memory.speed}`
  else
    p No memories found
